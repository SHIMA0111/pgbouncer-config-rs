[package]
name = "pgbouncer-generator"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"

pgbouncer-config = { version = "0.1" , path = "../pgbouncer-config" }